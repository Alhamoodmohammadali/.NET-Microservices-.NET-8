@model VMMaterialChapter
<form asp-action="UpSet" enctype="multipart/form-data">
    <br />
    <div class="container border p-3">
        <h1 class="text-white text-center">@((Model.Chapter.ChapterId == 0 || Model.Chapter.ChapterId == null) ? "Create" : "Edit") Chapter</h1>
        <input asp-for="Chapter.ChapterId" hidden />
        <hr />
        <div class="row">
            <div class="col-10">

                <div class="row">
                    <div class="col-2">
                        <label class="control-label pt-2" style="font-size:20px;"> Name</label>
                    </div>
                    <div class="col-10 pb-3">
                        <input asp-for="Chapter.Name" class="form-control" />
                        <span asp-validation-for="Chapter.Name" class="text-danger"></span>
                    </div>                 
                    <div class="col-2">
                        <label class="control-label pt-2" style="font-size:20px;">Description</label>
                    </div>
                    <div class="col-10 pb-3">
                        <textarea asp-for="Chapter.Description" class="form-control" rows="5"></textarea>
                        <span asp-validation-for="Chapter.Description" class="text-danger"></span>
                    </div>
                    <div class="form-floating py-2 col-12">
                        <select asp-for="@Model.Chapter.MaterialId" asp-items="@Model.MaterialList" class="form-select border-0 shadow">
                            <option disabled selected>--Select University--</option>
                        </select>
                        <label asp-for="Chapter.MaterialId" class="ms-2"></label>
                        <span asp-validation-for="Chapter.MaterialId" class="text-danger"></span>
                    </div>
     
                    <div class="col-2">
                        <label class="control-label pt-2" style="font-size:20px;">Pdf File</label>
                    </div>
                    <div class="col-10 pb-3">
                        <input asp-for="Chapter.Pdf" class="form-control" />
                        <input asp-for="Chapter.PdfUrl" hidden class="form-control" />
                        @if (!(Model.Chapter.ChapterId == 0 || Model.Chapter.ChapterId == null))
                        {
                            <input asp-for="Chapter.PdfLocalPath" hidden class="form-control" />
                        }
                        <span asp-validation-for="Chapter.Pdf" class="text-danger"></span>
                    </div>
                    <div class="col-5 offset-2">
                        <a asp-action="Index" class="btn-primary btn form-control ">Back to List</a>
                    </div>
                    <div class="col-5">
                        @if (Model.Chapter.ChapterId == 0 || Model.Chapter.ChapterId == null)
                        {
                            <input type="submit" value="Create" class="btn btn-danger form-control" />
                        }
                        else
                        {
                            <input type="submit" value="Edit" class="btn btn-danger form-control" />
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}